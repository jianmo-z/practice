//
// Created by pip on 19-3-10.
//
#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<vector<int>> generateMatrix(int n) {
        vector<vector<int>> ret(n);

        for(int i = 0; i < n; ++i) {
            for(int j = 0; j < n; ++j) {

            }
        }
    }

};

void test() {
    int n = 3;
    Solution s;
    s.generateMatrix(n);
}